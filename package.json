{
  "name": "pascaline-3d",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Modélisation 3D interactive et pédagogique du mécanisme de la Pascaline",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "three": "^0.181.0"
  },
  "devDependencies": {
    "@types/three": "^0.181.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
